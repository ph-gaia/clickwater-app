<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="br.com.clickwater">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".mvp.splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".mvp.signin.SigninActivity" />
        <activity android:name=".mvp.signup.SignupActivity" />
        <activity android:name=".mvp.home.HomeActivity" />
        <activity android:name=".mvp.seller.SellerActivity" />
        <activity android:name=".mvp.payment.PaymentActivity" />
        <activity android:name=".mvp.newAddress.NewAddressActivity" />
        <activity android:name=".mvp.address.AddressActivity" />
        <activity android:name=".mvp.orders.OrdersActivity" />
        <activity android:name=".mvp.newPayment.NewPaymentActivity" />
    </application>

</manifest>